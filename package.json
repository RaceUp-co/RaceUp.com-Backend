{
  "name": "raceup-backend-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute raceup-db --local --file=./db/schema.sql",
    "db:init:remote": "wrangler d1 execute raceup-db --remote --file=./db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@hono/zod-validator": "^0.5.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
